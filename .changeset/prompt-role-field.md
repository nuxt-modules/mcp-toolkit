---
"@nuxtjs/mcp-toolkit": minor
---

Add optional `role` field (`'user'` | `'assistant'`) to prompt definitions. When a handler returns a plain string, this role is used for the auto-wrapped message (defaults to `'user'`).
