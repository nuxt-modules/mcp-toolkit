---
"@nuxtjs/mcp-toolkit": minor
---

Migrate Code Mode to secure-exec 0.3 (`>=0.3.3` peer dependency) and replace the removed network adapter with an `mcp-rpc` host binding so sandbox tool calls still reach the dispatcher.
