---
"@nuxtjs/mcp-toolkit": minor
---

Add `description`, `instructions`, and `icons` to module options and `defineMcpHandler()`. `description` and `icons` are sent as part of `serverInfo` during MCP initialization for client UIs. `instructions` provides operational guidance for AI agents, typically injected into the model's system prompt.
