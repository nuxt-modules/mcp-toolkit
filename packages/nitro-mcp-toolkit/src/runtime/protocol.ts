/**
 * The protocol revision this toolkit targets.
 *
 * Exported because a client pins the revision it wants, and the SDK's own
 * `LATEST_PROTOCOL_VERSION` still names the last 2025-era one.
 */
export const MODERN_PROTOCOL_VERSION = '2026-07-28'
