/**
 * The protocol revision this toolkit targets.
 *
 * Restated here rather than imported from `h3-mcp/modern`, which would pull
 * that entrypoint into a file that already imports `h3-mcp` and defeat the
 * bundle split.
 */
export const MODERN_PROTOCOL_VERSION = '2026-07-28'
